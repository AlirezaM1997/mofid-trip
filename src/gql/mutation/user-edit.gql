mutation userEdit($data: UserEditInputType) {
  userEdit(data: $data) {
    ... on ResponseWithToken {
      status
      statusCode
      message
    }
    ... on ResponseBase {
      status
      statusCode
      message
    }
  }
}
