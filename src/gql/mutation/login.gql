mutation login($dataUser: UserInputType) {
  login(dataUser: $dataUser) {
    __typename
    ... on ResponseBase {
      status
      message
      statusCode
      metadata
      __typename
    }
    ... on ResponseWithToken {
      status
      statusCode
      message
      token
      refreshToken
      metadata
      __typename
    }
  }
}
